import 'package:flutter/material.dart';

// This file defines universal color constants for the application.

class AppColors {
  // Private constructor to prevent instantiation, as this class only holds static constants.
  AppColors._();

  // Define your custom colors here using 'static const'
  // Hex color value: #144a1a (fully opaque)
  static const Color primaryGreen = Color(0xFF144A1A);
  static const Color secondaryGreen = Color(0xFF1A6A1A);
  static const Color darkGrey = Color(0xFF333333); // Example dark grey
}
